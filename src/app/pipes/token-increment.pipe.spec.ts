import { TokenIncrementPipe } from './token-increment.pipe';

describe('TokenIncrementPipe', () => {
  it('create an instance', () => {
    const pipe = new TokenIncrementPipe();
    expect(pipe).toBeTruthy();
  });
});
